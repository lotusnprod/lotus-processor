stages:
  - docker

build_docker_image:
  stage: docker
  script:
    - make docker-build
  rules:
    - changes:
        - Dockerfile
      when: manual
      allow_failure: false

validate_docker_image:
  stage: docker
  script:
    - docker run -it --rm -v $PWD:/srv/onpdb onpdb-environment true
